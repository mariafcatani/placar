import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    boxRodada: {
        alignItems: 'center',
        borderBottomWidth: 1,
        borderTopWidth: 1,
        borderColor: '#666',
        marginTop: 10,
        paddingVertical: 10
    },
    textRodada: {
        fontSize: 22,
        fontWeight: 'bold'
    }
  });